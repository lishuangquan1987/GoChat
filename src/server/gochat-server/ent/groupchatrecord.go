// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"gochat_server/ent/groupchatrecord"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GroupChatRecord is the model entity for the GroupChatRecord schema.
type GroupChatRecord struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 消息ID,由发送者产生
	MsgId string `json:"msgId,omitempty"`
	// 发送者ID
	FromUserId string `json:"fromUserId,omitempty"`
	// 群组ID
	GroupId string `json:"groupId,omitempty"`
	// 消息类型
	MsgType string `json:"msgType,omitempty"`
	// 创建时间
	CreateTime   time.Time `json:"createTime,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GroupChatRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case groupchatrecord.FieldID:
			values[i] = new(sql.NullInt64)
		case groupchatrecord.FieldMsgId, groupchatrecord.FieldFromUserId, groupchatrecord.FieldGroupId, groupchatrecord.FieldMsgType:
			values[i] = new(sql.NullString)
		case groupchatrecord.FieldCreateTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GroupChatRecord fields.
func (gcr *GroupChatRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case groupchatrecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gcr.ID = int(value.Int64)
		case groupchatrecord.FieldMsgId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field msgId", values[i])
			} else if value.Valid {
				gcr.MsgId = value.String
			}
		case groupchatrecord.FieldFromUserId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field fromUserId", values[i])
			} else if value.Valid {
				gcr.FromUserId = value.String
			}
		case groupchatrecord.FieldGroupId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field groupId", values[i])
			} else if value.Valid {
				gcr.GroupId = value.String
			}
		case groupchatrecord.FieldMsgType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field msgType", values[i])
			} else if value.Valid {
				gcr.MsgType = value.String
			}
		case groupchatrecord.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createTime", values[i])
			} else if value.Valid {
				gcr.CreateTime = value.Time
			}
		default:
			gcr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GroupChatRecord.
// This includes values selected through modifiers, order, etc.
func (gcr *GroupChatRecord) Value(name string) (ent.Value, error) {
	return gcr.selectValues.Get(name)
}

// Update returns a builder for updating this GroupChatRecord.
// Note that you need to call GroupChatRecord.Unwrap() before calling this method if this GroupChatRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (gcr *GroupChatRecord) Update() *GroupChatRecordUpdateOne {
	return NewGroupChatRecordClient(gcr.config).UpdateOne(gcr)
}

// Unwrap unwraps the GroupChatRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gcr *GroupChatRecord) Unwrap() *GroupChatRecord {
	_tx, ok := gcr.config.driver.(*txDriver)
	if !ok {
		panic("ent: GroupChatRecord is not a transactional entity")
	}
	gcr.config.driver = _tx.drv
	return gcr
}

// String implements the fmt.Stringer.
func (gcr *GroupChatRecord) String() string {
	var builder strings.Builder
	builder.WriteString("GroupChatRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gcr.ID))
	builder.WriteString("msgId=")
	builder.WriteString(gcr.MsgId)
	builder.WriteString(", ")
	builder.WriteString("fromUserId=")
	builder.WriteString(gcr.FromUserId)
	builder.WriteString(", ")
	builder.WriteString("groupId=")
	builder.WriteString(gcr.GroupId)
	builder.WriteString(", ")
	builder.WriteString("msgType=")
	builder.WriteString(gcr.MsgType)
	builder.WriteString(", ")
	builder.WriteString("createTime=")
	builder.WriteString(gcr.CreateTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GroupChatRecords is a parsable slice of GroupChatRecord.
type GroupChatRecords []*GroupChatRecord
