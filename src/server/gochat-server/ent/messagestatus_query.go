// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"gochat_server/ent/messagestatus"
	"gochat_server/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MessageStatusQuery is the builder for querying MessageStatus entities.
type MessageStatusQuery struct {
	config
	ctx        *QueryContext
	order      []messagestatus.OrderOption
	inters     []Interceptor
	predicates []predicate.MessageStatus
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MessageStatusQuery builder.
func (msq *MessageStatusQuery) Where(ps ...predicate.MessageStatus) *MessageStatusQuery {
	msq.predicates = append(msq.predicates, ps...)
	return msq
}

// Limit the number of records to be returned by this query.
func (msq *MessageStatusQuery) Limit(limit int) *MessageStatusQuery {
	msq.ctx.Limit = &limit
	return msq
}

// Offset to start from.
func (msq *MessageStatusQuery) Offset(offset int) *MessageStatusQuery {
	msq.ctx.Offset = &offset
	return msq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (msq *MessageStatusQuery) Unique(unique bool) *MessageStatusQuery {
	msq.ctx.Unique = &unique
	return msq
}

// Order specifies how the records should be ordered.
func (msq *MessageStatusQuery) Order(o ...messagestatus.OrderOption) *MessageStatusQuery {
	msq.order = append(msq.order, o...)
	return msq
}

// First returns the first MessageStatus entity from the query.
// Returns a *NotFoundError when no MessageStatus was found.
func (msq *MessageStatusQuery) First(ctx context.Context) (*MessageStatus, error) {
	nodes, err := msq.Limit(1).All(setContextOp(ctx, msq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{messagestatus.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (msq *MessageStatusQuery) FirstX(ctx context.Context) *MessageStatus {
	node, err := msq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MessageStatus ID from the query.
// Returns a *NotFoundError when no MessageStatus ID was found.
func (msq *MessageStatusQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = msq.Limit(1).IDs(setContextOp(ctx, msq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{messagestatus.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (msq *MessageStatusQuery) FirstIDX(ctx context.Context) int {
	id, err := msq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MessageStatus entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MessageStatus entity is found.
// Returns a *NotFoundError when no MessageStatus entities are found.
func (msq *MessageStatusQuery) Only(ctx context.Context) (*MessageStatus, error) {
	nodes, err := msq.Limit(2).All(setContextOp(ctx, msq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{messagestatus.Label}
	default:
		return nil, &NotSingularError{messagestatus.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (msq *MessageStatusQuery) OnlyX(ctx context.Context) *MessageStatus {
	node, err := msq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MessageStatus ID in the query.
// Returns a *NotSingularError when more than one MessageStatus ID is found.
// Returns a *NotFoundError when no entities are found.
func (msq *MessageStatusQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = msq.Limit(2).IDs(setContextOp(ctx, msq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{messagestatus.Label}
	default:
		err = &NotSingularError{messagestatus.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (msq *MessageStatusQuery) OnlyIDX(ctx context.Context) int {
	id, err := msq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MessageStatusSlice.
func (msq *MessageStatusQuery) All(ctx context.Context) ([]*MessageStatus, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryAll)
	if err := msq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MessageStatus, *MessageStatusQuery]()
	return withInterceptors[[]*MessageStatus](ctx, msq, qr, msq.inters)
}

// AllX is like All, but panics if an error occurs.
func (msq *MessageStatusQuery) AllX(ctx context.Context) []*MessageStatus {
	nodes, err := msq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MessageStatus IDs.
func (msq *MessageStatusQuery) IDs(ctx context.Context) (ids []int, err error) {
	if msq.ctx.Unique == nil && msq.path != nil {
		msq.Unique(true)
	}
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryIDs)
	if err = msq.Select(messagestatus.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (msq *MessageStatusQuery) IDsX(ctx context.Context) []int {
	ids, err := msq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (msq *MessageStatusQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryCount)
	if err := msq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, msq, querierCount[*MessageStatusQuery](), msq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (msq *MessageStatusQuery) CountX(ctx context.Context) int {
	count, err := msq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (msq *MessageStatusQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryExist)
	switch _, err := msq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (msq *MessageStatusQuery) ExistX(ctx context.Context) bool {
	exist, err := msq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MessageStatusQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (msq *MessageStatusQuery) Clone() *MessageStatusQuery {
	if msq == nil {
		return nil
	}
	return &MessageStatusQuery{
		config:     msq.config,
		ctx:        msq.ctx.Clone(),
		order:      append([]messagestatus.OrderOption{}, msq.order...),
		inters:     append([]Interceptor{}, msq.inters...),
		predicates: append([]predicate.MessageStatus{}, msq.predicates...),
		// clone intermediate query.
		sql:  msq.sql.Clone(),
		path: msq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MsgId string `json:"msgId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MessageStatus.Query().
//		GroupBy(messagestatus.FieldMsgId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (msq *MessageStatusQuery) GroupBy(field string, fields ...string) *MessageStatusGroupBy {
	msq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MessageStatusGroupBy{build: msq}
	grbuild.flds = &msq.ctx.Fields
	grbuild.label = messagestatus.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MsgId string `json:"msgId,omitempty"`
//	}
//
//	client.MessageStatus.Query().
//		Select(messagestatus.FieldMsgId).
//		Scan(ctx, &v)
func (msq *MessageStatusQuery) Select(fields ...string) *MessageStatusSelect {
	msq.ctx.Fields = append(msq.ctx.Fields, fields...)
	sbuild := &MessageStatusSelect{MessageStatusQuery: msq}
	sbuild.label = messagestatus.Label
	sbuild.flds, sbuild.scan = &msq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MessageStatusSelect configured with the given aggregations.
func (msq *MessageStatusQuery) Aggregate(fns ...AggregateFunc) *MessageStatusSelect {
	return msq.Select().Aggregate(fns...)
}

func (msq *MessageStatusQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range msq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, msq); err != nil {
				return err
			}
		}
	}
	for _, f := range msq.ctx.Fields {
		if !messagestatus.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if msq.path != nil {
		prev, err := msq.path(ctx)
		if err != nil {
			return err
		}
		msq.sql = prev
	}
	return nil
}

func (msq *MessageStatusQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MessageStatus, error) {
	var (
		nodes = []*MessageStatus{}
		_spec = msq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MessageStatus).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MessageStatus{config: msq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, msq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (msq *MessageStatusQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := msq.querySpec()
	_spec.Node.Columns = msq.ctx.Fields
	if len(msq.ctx.Fields) > 0 {
		_spec.Unique = msq.ctx.Unique != nil && *msq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, msq.driver, _spec)
}

func (msq *MessageStatusQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(messagestatus.Table, messagestatus.Columns, sqlgraph.NewFieldSpec(messagestatus.FieldID, field.TypeInt))
	_spec.From = msq.sql
	if unique := msq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if msq.path != nil {
		_spec.Unique = true
	}
	if fields := msq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, messagestatus.FieldID)
		for i := range fields {
			if fields[i] != messagestatus.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := msq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := msq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := msq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := msq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (msq *MessageStatusQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(msq.driver.Dialect())
	t1 := builder.Table(messagestatus.Table)
	columns := msq.ctx.Fields
	if len(columns) == 0 {
		columns = messagestatus.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if msq.sql != nil {
		selector = msq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if msq.ctx.Unique != nil && *msq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range msq.predicates {
		p(selector)
	}
	for _, p := range msq.order {
		p(selector)
	}
	if offset := msq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := msq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MessageStatusGroupBy is the group-by builder for MessageStatus entities.
type MessageStatusGroupBy struct {
	selector
	build *MessageStatusQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (msgb *MessageStatusGroupBy) Aggregate(fns ...AggregateFunc) *MessageStatusGroupBy {
	msgb.fns = append(msgb.fns, fns...)
	return msgb
}

// Scan applies the selector query and scans the result into the given value.
func (msgb *MessageStatusGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, msgb.build.ctx, ent.OpQueryGroupBy)
	if err := msgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MessageStatusQuery, *MessageStatusGroupBy](ctx, msgb.build, msgb, msgb.build.inters, v)
}

func (msgb *MessageStatusGroupBy) sqlScan(ctx context.Context, root *MessageStatusQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(msgb.fns))
	for _, fn := range msgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*msgb.flds)+len(msgb.fns))
		for _, f := range *msgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*msgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := msgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MessageStatusSelect is the builder for selecting fields of MessageStatus entities.
type MessageStatusSelect struct {
	*MessageStatusQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mss *MessageStatusSelect) Aggregate(fns ...AggregateFunc) *MessageStatusSelect {
	mss.fns = append(mss.fns, fns...)
	return mss
}

// Scan applies the selector query and scans the result into the given value.
func (mss *MessageStatusSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mss.ctx, ent.OpQuerySelect)
	if err := mss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MessageStatusQuery, *MessageStatusSelect](ctx, mss.MessageStatusQuery, mss, mss.inters, v)
}

func (mss *MessageStatusSelect) sqlScan(ctx context.Context, root *MessageStatusQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mss.fns))
	for _, fn := range mss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
