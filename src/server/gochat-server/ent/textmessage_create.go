// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"gochat_server/ent/textmessage"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TextMessageCreate is the builder for creating a TextMessage entity.
type TextMessageCreate struct {
	config
	mutation *TextMessageMutation
	hooks    []Hook
}

// SetMsgId sets the "msgId" field.
func (tmc *TextMessageCreate) SetMsgId(s string) *TextMessageCreate {
	tmc.mutation.SetMsgId(s)
	return tmc
}

// SetText sets the "text" field.
func (tmc *TextMessageCreate) SetText(s string) *TextMessageCreate {
	tmc.mutation.SetText(s)
	return tmc
}

// Mutation returns the TextMessageMutation object of the builder.
func (tmc *TextMessageCreate) Mutation() *TextMessageMutation {
	return tmc.mutation
}

// Save creates the TextMessage in the database.
func (tmc *TextMessageCreate) Save(ctx context.Context) (*TextMessage, error) {
	return withHooks(ctx, tmc.sqlSave, tmc.mutation, tmc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tmc *TextMessageCreate) SaveX(ctx context.Context) *TextMessage {
	v, err := tmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tmc *TextMessageCreate) Exec(ctx context.Context) error {
	_, err := tmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tmc *TextMessageCreate) ExecX(ctx context.Context) {
	if err := tmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tmc *TextMessageCreate) check() error {
	if _, ok := tmc.mutation.MsgId(); !ok {
		return &ValidationError{Name: "msgId", err: errors.New(`ent: missing required field "TextMessage.msgId"`)}
	}
	if v, ok := tmc.mutation.MsgId(); ok {
		if err := textmessage.MsgIdValidator(v); err != nil {
			return &ValidationError{Name: "msgId", err: fmt.Errorf(`ent: validator failed for field "TextMessage.msgId": %w`, err)}
		}
	}
	if _, ok := tmc.mutation.Text(); !ok {
		return &ValidationError{Name: "text", err: errors.New(`ent: missing required field "TextMessage.text"`)}
	}
	if v, ok := tmc.mutation.Text(); ok {
		if err := textmessage.TextValidator(v); err != nil {
			return &ValidationError{Name: "text", err: fmt.Errorf(`ent: validator failed for field "TextMessage.text": %w`, err)}
		}
	}
	return nil
}

func (tmc *TextMessageCreate) sqlSave(ctx context.Context) (*TextMessage, error) {
	if err := tmc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	tmc.mutation.id = &_node.ID
	tmc.mutation.done = true
	return _node, nil
}

func (tmc *TextMessageCreate) createSpec() (*TextMessage, *sqlgraph.CreateSpec) {
	var (
		_node = &TextMessage{config: tmc.config}
		_spec = sqlgraph.NewCreateSpec(textmessage.Table, sqlgraph.NewFieldSpec(textmessage.FieldID, field.TypeInt))
	)
	if value, ok := tmc.mutation.MsgId(); ok {
		_spec.SetField(textmessage.FieldMsgId, field.TypeString, value)
		_node.MsgId = value
	}
	if value, ok := tmc.mutation.Text(); ok {
		_spec.SetField(textmessage.FieldText, field.TypeString, value)
		_node.Text = value
	}
	return _node, _spec
}

// TextMessageCreateBulk is the builder for creating many TextMessage entities in bulk.
type TextMessageCreateBulk struct {
	config
	err      error
	builders []*TextMessageCreate
}

// Save creates the TextMessage entities in the database.
func (tmcb *TextMessageCreateBulk) Save(ctx context.Context) ([]*TextMessage, error) {
	if tmcb.err != nil {
		return nil, tmcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tmcb.builders))
	nodes := make([]*TextMessage, len(tmcb.builders))
	mutators := make([]Mutator, len(tmcb.builders))
	for i := range tmcb.builders {
		func(i int, root context.Context) {
			builder := tmcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TextMessageMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tmcb *TextMessageCreateBulk) SaveX(ctx context.Context) []*TextMessage {
	v, err := tmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tmcb *TextMessageCreateBulk) Exec(ctx context.Context) error {
	_, err := tmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tmcb *TextMessageCreateBulk) ExecX(ctx context.Context) {
	if err := tmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
