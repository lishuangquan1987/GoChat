// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"gochat_server/ent/donotdisturb"
	"gochat_server/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DoNotDisturbQuery is the builder for querying DoNotDisturb entities.
type DoNotDisturbQuery struct {
	config
	ctx        *QueryContext
	order      []donotdisturb.OrderOption
	inters     []Interceptor
	predicates []predicate.DoNotDisturb
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DoNotDisturbQuery builder.
func (dndq *DoNotDisturbQuery) Where(ps ...predicate.DoNotDisturb) *DoNotDisturbQuery {
	dndq.predicates = append(dndq.predicates, ps...)
	return dndq
}

// Limit the number of records to be returned by this query.
func (dndq *DoNotDisturbQuery) Limit(limit int) *DoNotDisturbQuery {
	dndq.ctx.Limit = &limit
	return dndq
}

// Offset to start from.
func (dndq *DoNotDisturbQuery) Offset(offset int) *DoNotDisturbQuery {
	dndq.ctx.Offset = &offset
	return dndq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dndq *DoNotDisturbQuery) Unique(unique bool) *DoNotDisturbQuery {
	dndq.ctx.Unique = &unique
	return dndq
}

// Order specifies how the records should be ordered.
func (dndq *DoNotDisturbQuery) Order(o ...donotdisturb.OrderOption) *DoNotDisturbQuery {
	dndq.order = append(dndq.order, o...)
	return dndq
}

// First returns the first DoNotDisturb entity from the query.
// Returns a *NotFoundError when no DoNotDisturb was found.
func (dndq *DoNotDisturbQuery) First(ctx context.Context) (*DoNotDisturb, error) {
	nodes, err := dndq.Limit(1).All(setContextOp(ctx, dndq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{donotdisturb.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) FirstX(ctx context.Context) *DoNotDisturb {
	node, err := dndq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DoNotDisturb ID from the query.
// Returns a *NotFoundError when no DoNotDisturb ID was found.
func (dndq *DoNotDisturbQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dndq.Limit(1).IDs(setContextOp(ctx, dndq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{donotdisturb.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) FirstIDX(ctx context.Context) int {
	id, err := dndq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DoNotDisturb entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DoNotDisturb entity is found.
// Returns a *NotFoundError when no DoNotDisturb entities are found.
func (dndq *DoNotDisturbQuery) Only(ctx context.Context) (*DoNotDisturb, error) {
	nodes, err := dndq.Limit(2).All(setContextOp(ctx, dndq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{donotdisturb.Label}
	default:
		return nil, &NotSingularError{donotdisturb.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) OnlyX(ctx context.Context) *DoNotDisturb {
	node, err := dndq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DoNotDisturb ID in the query.
// Returns a *NotSingularError when more than one DoNotDisturb ID is found.
// Returns a *NotFoundError when no entities are found.
func (dndq *DoNotDisturbQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dndq.Limit(2).IDs(setContextOp(ctx, dndq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{donotdisturb.Label}
	default:
		err = &NotSingularError{donotdisturb.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) OnlyIDX(ctx context.Context) int {
	id, err := dndq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DoNotDisturbs.
func (dndq *DoNotDisturbQuery) All(ctx context.Context) ([]*DoNotDisturb, error) {
	ctx = setContextOp(ctx, dndq.ctx, ent.OpQueryAll)
	if err := dndq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DoNotDisturb, *DoNotDisturbQuery]()
	return withInterceptors[[]*DoNotDisturb](ctx, dndq, qr, dndq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) AllX(ctx context.Context) []*DoNotDisturb {
	nodes, err := dndq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DoNotDisturb IDs.
func (dndq *DoNotDisturbQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dndq.ctx.Unique == nil && dndq.path != nil {
		dndq.Unique(true)
	}
	ctx = setContextOp(ctx, dndq.ctx, ent.OpQueryIDs)
	if err = dndq.Select(donotdisturb.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) IDsX(ctx context.Context) []int {
	ids, err := dndq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dndq *DoNotDisturbQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dndq.ctx, ent.OpQueryCount)
	if err := dndq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dndq, querierCount[*DoNotDisturbQuery](), dndq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) CountX(ctx context.Context) int {
	count, err := dndq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dndq *DoNotDisturbQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dndq.ctx, ent.OpQueryExist)
	switch _, err := dndq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dndq *DoNotDisturbQuery) ExistX(ctx context.Context) bool {
	exist, err := dndq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DoNotDisturbQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dndq *DoNotDisturbQuery) Clone() *DoNotDisturbQuery {
	if dndq == nil {
		return nil
	}
	return &DoNotDisturbQuery{
		config:     dndq.config,
		ctx:        dndq.ctx.Clone(),
		order:      append([]donotdisturb.OrderOption{}, dndq.order...),
		inters:     append([]Interceptor{}, dndq.inters...),
		predicates: append([]predicate.DoNotDisturb{}, dndq.predicates...),
		// clone intermediate query.
		sql:  dndq.sql.Clone(),
		path: dndq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DoNotDisturb.Query().
//		GroupBy(donotdisturb.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dndq *DoNotDisturbQuery) GroupBy(field string, fields ...string) *DoNotDisturbGroupBy {
	dndq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DoNotDisturbGroupBy{build: dndq}
	grbuild.flds = &dndq.ctx.Fields
	grbuild.label = donotdisturb.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.DoNotDisturb.Query().
//		Select(donotdisturb.FieldUserID).
//		Scan(ctx, &v)
func (dndq *DoNotDisturbQuery) Select(fields ...string) *DoNotDisturbSelect {
	dndq.ctx.Fields = append(dndq.ctx.Fields, fields...)
	sbuild := &DoNotDisturbSelect{DoNotDisturbQuery: dndq}
	sbuild.label = donotdisturb.Label
	sbuild.flds, sbuild.scan = &dndq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DoNotDisturbSelect configured with the given aggregations.
func (dndq *DoNotDisturbQuery) Aggregate(fns ...AggregateFunc) *DoNotDisturbSelect {
	return dndq.Select().Aggregate(fns...)
}

func (dndq *DoNotDisturbQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dndq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dndq); err != nil {
				return err
			}
		}
	}
	for _, f := range dndq.ctx.Fields {
		if !donotdisturb.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dndq.path != nil {
		prev, err := dndq.path(ctx)
		if err != nil {
			return err
		}
		dndq.sql = prev
	}
	return nil
}

func (dndq *DoNotDisturbQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DoNotDisturb, error) {
	var (
		nodes = []*DoNotDisturb{}
		_spec = dndq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DoNotDisturb).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DoNotDisturb{config: dndq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dndq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dndq *DoNotDisturbQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dndq.querySpec()
	_spec.Node.Columns = dndq.ctx.Fields
	if len(dndq.ctx.Fields) > 0 {
		_spec.Unique = dndq.ctx.Unique != nil && *dndq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dndq.driver, _spec)
}

func (dndq *DoNotDisturbQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(donotdisturb.Table, donotdisturb.Columns, sqlgraph.NewFieldSpec(donotdisturb.FieldID, field.TypeInt))
	_spec.From = dndq.sql
	if unique := dndq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dndq.path != nil {
		_spec.Unique = true
	}
	if fields := dndq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, donotdisturb.FieldID)
		for i := range fields {
			if fields[i] != donotdisturb.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dndq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dndq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dndq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dndq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dndq *DoNotDisturbQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dndq.driver.Dialect())
	t1 := builder.Table(donotdisturb.Table)
	columns := dndq.ctx.Fields
	if len(columns) == 0 {
		columns = donotdisturb.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dndq.sql != nil {
		selector = dndq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dndq.ctx.Unique != nil && *dndq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dndq.predicates {
		p(selector)
	}
	for _, p := range dndq.order {
		p(selector)
	}
	if offset := dndq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dndq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DoNotDisturbGroupBy is the group-by builder for DoNotDisturb entities.
type DoNotDisturbGroupBy struct {
	selector
	build *DoNotDisturbQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dndgb *DoNotDisturbGroupBy) Aggregate(fns ...AggregateFunc) *DoNotDisturbGroupBy {
	dndgb.fns = append(dndgb.fns, fns...)
	return dndgb
}

// Scan applies the selector query and scans the result into the given value.
func (dndgb *DoNotDisturbGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dndgb.build.ctx, ent.OpQueryGroupBy)
	if err := dndgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DoNotDisturbQuery, *DoNotDisturbGroupBy](ctx, dndgb.build, dndgb, dndgb.build.inters, v)
}

func (dndgb *DoNotDisturbGroupBy) sqlScan(ctx context.Context, root *DoNotDisturbQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dndgb.fns))
	for _, fn := range dndgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dndgb.flds)+len(dndgb.fns))
		for _, f := range *dndgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dndgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dndgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DoNotDisturbSelect is the builder for selecting fields of DoNotDisturb entities.
type DoNotDisturbSelect struct {
	*DoNotDisturbQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dnds *DoNotDisturbSelect) Aggregate(fns ...AggregateFunc) *DoNotDisturbSelect {
	dnds.fns = append(dnds.fns, fns...)
	return dnds
}

// Scan applies the selector query and scans the result into the given value.
func (dnds *DoNotDisturbSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dnds.ctx, ent.OpQuerySelect)
	if err := dnds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DoNotDisturbQuery, *DoNotDisturbSelect](ctx, dnds.DoNotDisturbQuery, dnds, dnds.inters, v)
}

func (dnds *DoNotDisturbSelect) sqlScan(ctx context.Context, root *DoNotDisturbQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dnds.fns))
	for _, fn := range dnds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dnds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dnds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
